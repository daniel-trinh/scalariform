package scalariform.lexer

object CharConstants {

  /**
   * The substitute character (␚) is a control character that is used in the
   * place of a character that is recognized to be invalid or in error or that
   * cannot be represented on a given device.
   * It is also used as an escape sequence for some programming languages.
   */
  final val SU = '\u001A'
}
